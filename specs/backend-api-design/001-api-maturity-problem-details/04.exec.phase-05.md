# Execution Phase 05 - Update Agent and Validation Guidance

## Phase Scope

Updated AI-assisted development guidance so API contract work routes to the new HTTP API design baseline.

## Changes Made

- Updated `ai-assisted-development/context-guides/backend-vsa-minimal-api.md` to include:
  - `HTTP API Design` in relevant references;
  - resource-oriented route, status-code and Problem Details expectations;
  - integration validation guidance for status code and `application/problem+json` shape when error behavior changes.
- Updated `ai-assisted-development/agent-routing.md` with a routing row for HTTP API contracts, status codes and error responses.

## Validation Evidence

Guidance was reviewed for:

- Minimal context routing.
- Consistency with the new API design page.
- Avoiding unnecessary changes to testing pages already covering HTTP integration behavior.

Build validation is recorded in Phase 06.

## Notes

- `technologies/testing/` already covers integration tests and evidence, so no testing page changes were needed in this phase.

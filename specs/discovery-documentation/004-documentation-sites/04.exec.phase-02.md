# Ejecución Fase 2 — Actualización de Folder Structure

## Objetivo de la Fase

Reescribir el bloque `Folder Structure` en `layers/index.md` para reflejar la nueva convención de organizar las capas disciplinarias bajo un directorio `layers/`, con todas las carpetas y archivos ordenados alfabéticamente.

## Cambios Realizados

### `discovery/documentation/layers/index.md`

Bloque `## Folder Structure` reescrito. Cambios respecto a la versión anterior:

- **Texto introductorio** — de "expose the durable project layers directly, instead of mirroring the structure of this guide" a "group the durable project layers under a `layers/` directory, instead of placing them directly at the root".
- **Estructura del árbol** — las cinco carpetas de capa (`analysis/`, `architecture/`, `delivery/`, `strategy/`, `validation/`) pasan a ser hijas de `layers/` dentro del ejemplo.
- **Orden alfabético** — todas las carpetas y archivos dentro de cada nivel reorganizados alfabéticamente:
  - Capas: `analysis/`, `architecture/`, `delivery/`, `strategy/`, `validation/`
  - Subcarpetas de `analysis/`: `business-rules/`, `needs/`, `requirements/`, `use-cases/`
  - Subcarpetas de `architecture/`: `contracts/`, `decisions/`
  - Subcarpetas de `delivery/`: `epics/`, `pbi/`
  - Subcarpetas de `strategy/`: `initiatives/`, `themes/`
  - Subcarpetas de `validation/`: `acceptance-criteria/`, `scenarios/`, `test-cases/`
- **`static-site-generators/` excluido** — per Revisión 01: no corresponde sugerir esa carpeta en la estructura de un proyecto de documentación.

## Evidencia y Validación

Build pasó sin errores en Fase 1; no se ejecutó build adicional al final de esta fase porque el cambio es exclusivamente editorial (sin links nuevos ni rutas modificadas). La revisión visual del árbol confirma el orden alfabético correcto y la estructura anidada esperada.

## Artefactos Generados

| Artefacto | Estado |
| --- | --- |
| `discovery/documentation/layers/index.md` | Actualizado |

# Plan — Reubicar Writing Guidelines Fuera de `layers/`

## Estrategia General

Ejecutar la reubicación en fases secuenciales cortas: primero mover el archivo, luego actualizar las referencias en documentos Markdown y finalmente actualizar `config.ts` y `conventions/index.md`. Validar con `npm run docs:build` al final.

## Fases

### Fase 1 — Mover el archivo

- Mover `discovery/documentation/layers/writing-guidelines.md` a `conventions/writing-guidelines.md`.

### Fase 2 — Actualizar referencias en archivos Markdown

Actualizar los links en los siguientes archivos para apuntar a `/conventions/writing-guidelines`:

- `ai-assisted-development/context-guides/project-documentation-artifact.md` — 2 ocurrencias
- `ai-assisted-development/context-guides/repository-agent-instructions.md` — 1 ocurrencia
- `discovery/documentation/layers/index.md` — 2 ocurrencias (frontmatter `actions` + cuerpo)
- `conventions/repository-documentation.md` — 1 ocurrencia
- `AGENTS.md` — actualizar la referencia de texto plano de `discovery/documentation/layers/writing-guidelines.md` a `conventions/writing-guidelines.md`

### Fase 3 — Actualizar `config.ts`

**Nav:**

- Mover la entrada `{ text: 'Writing Guidelines', link: '/discovery/documentation/layers/writing-guidelines' }` del grupo "Discovery & Design" al grupo "Conventions", con el link actualizado a `/conventions/writing-guidelines`.

**Sidebar:**

- Eliminar `{ text: 'Writing Guidelines', link: '/discovery/documentation/layers/writing-guidelines' }` del subárbol "Disciplinary Layers".
- Agregar `{ text: 'Writing Guidelines', link: '/conventions/writing-guidelines' }` dentro del grupo "Conventions" del sidebar, en la posición que corresponda alfabéticamente o al final de los ítems existentes.

### Fase 4 — Actualizar `conventions/index.md`

- Agregar una entrada para Writing Guidelines en la sección `features` del frontmatter:

  ```yaml
  - icon: ✍️
    title: Writing Guidelines
    details: Editorial rules for titles, tone, terminology and Markdown structure across repos and documentation sites.
    link: /conventions/writing-guidelines
  ```

- Agregar la entrada al listado de la sección `## Available Guides`:

  ```markdown
  - [Writing Guidelines](./writing-guidelines) — Editorial rules for titles, tone, terminology and Markdown structure.
  ```

## Validaciones Planificadas

| Fase | Validación |
| --- | --- |
| Fase 1 | Confirmar que el archivo existe en `conventions/writing-guidelines.md` y que ya no existe en `discovery/documentation/layers/` |
| Fase 2 | Revisar que ningún archivo Markdown del sitio (excluidos `specs/` y `.vitepress/dist/`) referencie la ruta anterior |
| Fase 3 | Verificar visualmente que "Writing Guidelines" aparece bajo "Conventions" en el nav y en el sidebar, y que ya no aparece bajo "Discovery & Design" |
| Fase 4 | Verificar que `conventions/index.md` muestra la nueva tarjeta y el listado correctamente |
| Final | `npm run docs:build` sin errores ni dead links |

> [!WARNING] Importante
> No ejecutar ninguna fase sin revisión y aprobación previa del desarrollador responsable.

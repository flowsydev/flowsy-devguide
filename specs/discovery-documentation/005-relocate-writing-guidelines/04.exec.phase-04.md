# Ejecución Fase 4 — Actualizar `conventions/index.md`

## Acciones

- Agregada tarjeta en la sección `features` del frontmatter (después de "Repository Documentation"):

  ```yaml
  - icon: ✍️
    title: Writing Guidelines
    details: Editorial rules for titles, tone, terminology and Markdown structure across repos and documentation sites.
    link: /conventions/writing-guidelines
  ```

- Agregada entrada en `## Available Guides` (después de "Repository Documentation"):

  ```markdown
  - [Writing Guidelines](./writing-guidelines) — Editorial rules for titles, tone, terminology and Markdown structure.
  ```

## Validación Final

`npm run docs:build` completó sin errores ni dead links. Tiempo de build: 6.85 s.

Verificación de referencias residuales con `grep -r "layers/writing-guidelines\|documentation/writing-guidelines"` (excluyendo `specs/` y `.vitepress/dist/`): sin resultados.

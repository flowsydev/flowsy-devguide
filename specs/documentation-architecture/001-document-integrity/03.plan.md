# 001 — Plan

## Phases

### Phase 1 — Known Defects

- Correct `/layers/` paths on the five layer landings.
- Remove consecutive duplicates in Testing and the Vue context guide.
- Update Vue context guide to `routing/` and physical `stores/` where appropriate.
- Align UI/API adaptation policy to one owner page with summaries elsewhere.

### Phase 2 — Section Maps

- Diff `.vitepress/config.ts` against landings and indexes.
- Complete Technologies and related maps so every sidebar page appears.

### Phase 3 — Link Validator

- Add `scripts/validate-documentation.mjs`.
- Expose `npm run docs:validate`.
- Cover relative Markdown links, absolute internal paths and frontmatter `link` fields.
- Ignore externals and empty anchors without hiding internal errors.

### Phase 4 — Validation

- Run `npm run docs:validate` and `npm run docs:build`.
- Spot-check the five layer landings.
- Confirm no file moves occurred.

## Validation Commands

```bash
npm run docs:validate
npm run docs:build
git diff --check
```

# 002 — Plan

## Phases

### Phase 1 — Architectural Decisions

- Confirm six primary areas plus Home.
- Keep English physical slugs and English Title Case labels.
- Adopt bridge pages with `type: redirect` and `redirect` for compatibility.

### Phase 2 — Identifier Catalog

- Inventory existing codes.
- Publish non-translatable catalog: `STR-*`, `ANL-*`, `ARC-*`, `DLV-*`, `VAL-*`.
- Align layers guide and templates; document transition for historical codes.

### Phase 3 — Metadata Contracts

- Define required and optional fields for landing, guide, profile, reference, template, context-guide and redirect page types.
- Provide valid examples.

### Phase 4 — Canonical Sources and Classification

- Assign owners for language, artifact IDs, date/time, auditing/validity, public IDs, errors, validation, Outbox/reliable delivery, testing strategy, writing guidelines, agent skills and dependency safety.
- Classify overloaded pages for later splits.

## Validation Commands

```bash
npm run docs:validate
npm run docs:build
```

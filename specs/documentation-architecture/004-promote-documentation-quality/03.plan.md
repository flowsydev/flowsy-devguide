# 004 — Plan

## Phases

### Phase 1 — Prepare Targets

- Create Documentation and Quality landings, sidebars and bridge stubs.
- Record old → new route matrix.

### Phase 2 — Migrate Documentation Family

- Move layers/templates to `documentation/project-artifacts/`.
- Move VitePress to `documentation/tooling/vitepress/`.
- Move repository documentation to `documentation/repositories/`.
- Move public Specs-Driven Development to `documentation/work-specs/`.
- Update AI context guides to link the new canonical sources.

### Phase 3 — Migrate Quality

- Move the Testing block to `quality/` preserving strategy → levels → evidence → stack/system profiles.
- Update navigation and related links.

### Phase 4 — Compatibility Close

- Validate old and new routes.
- Remove non-bridge duplicates.
- Record visible moves in `CHANGELOG.md`.

## Validation Commands

```bash
npm run docs:validate
npm run docs:build
```

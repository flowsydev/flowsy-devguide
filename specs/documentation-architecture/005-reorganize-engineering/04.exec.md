# 005 — Execution Record

## Phase 1 — Engineering Skeleton

- Intent: establish Engineering as the technical solutions area.
- Changes: created `content/engineering/index.md` and sub-area landings; prepared Technologies → Engineering bridge matrix.
- Bridges: previous `/technologies/**` paths stubbed as redirects.

## Phase 2 — Backend

- Intent: clear Backend ownership.
- Changes: `design-baseline.md`, `architecture/{vertical-slice,clean}`, `api/http-api-design.md`, `reliability/*`, `dotnet/{csharp,minimal-apis,background-services}`.
- Notes: Background Services remain .NET-local with Messaging links.

## Phase 3 — Data and Messaging

- Intent: independent entry for persistence and events.
- Changes:
  - Data: relational modeling, migrations (`concepts`, tools/strategies, `flwdb-cli`), database engines
  - Messaging: EDA, reliable delivery, Event Sourcing, Kafka/Redpanda event store
- Bridges: previous backend-nested data and messaging routes.

## Phase 4 — Frontend, Security and Cross-Cutting

- Intent: complete Engineering ownership map.
- Changes: Frontend Modular Architecture and Vue tree; `security/dependency-safety.md`; Cross-Cutting placeholders for date/time, auditing/validity and identifiers.
- Consumers: repository documentation now links Security instead of owning supply-chain detail.

## Phase 5 — Validation

- Intent: no leftover Technologies ownership in canonical content.
- Changes: context guides and navigation updated; CHANGELOG records Engineering promotion.
- Validation: `npm run docs:validate`, `npm run docs:build`, search for residual `/technologies/` references outside bridges.

## Notes

Progressive splits of Minimal APIs, Vue and reliability pages are deferred to 006. Full profile consolidation is deferred to 007.

# 006 — Plan

## Phases

### Phase 1 — Error Handling

- Split into error taxonomy/propagation, validation/invariants and transactional consistency.
- Move messaging resilience detail toward Messaging / Reliable Delivery.
- Keep Problem Details with HTTP API design.

### Phase 2 — Domain-Driven Design

- Split into overview, Bounded Contexts, Entities/Value Objects, Aggregates and DCB.
- Move `State`/`StateHandler` mapping to VSA.
- Move persistence conventions toward Cross-Cutting.

### Phase 3 — Minimal APIs

- Progressive path: structure, endpoints, commands/queries, State decision, examples.
- Reduce Mediator vs direct duplication via focused differences.

### Phase 4 — Vue Ecosystem

- Progressive path: components, structure, state/composables, UI/API contracts, Storybook/visual design, conventions.
- Move skills to AI context guide; testing ownership to Quality.

### Phase 5 — Project Documentation

- Separate vision/layers, organization, identifiers/traceability and templates.

### Phase 6 — Specs-Driven Development

- Separate vision, workflow and document reference; agent usage stays in context guide.

### Phase 7 — VitePress

- Separate start/configuration, layouts/navigation and deployment; keep DevGuide-specific detail as reference where needed.

### Phase 8 — Migration Tools

- Separate comparative criteria from tool-specific recipes; keep `flwdb` CLI page.

### Phase 9 — Journey Review

- Validate reading paths, bridges and references.

## Validation Commands

```bash
npm run docs:validate
npm run docs:build
```

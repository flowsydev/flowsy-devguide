# 008 — Plan

## Phases

### Phase 1 — Reconstruct Final State

- Collect routes, landings, sidebars, canonical sources and bridges from 001–007 summaries.
- Confirm Discovery and Technologies hold bridges only.

### Phase 2 — Operational Documents

- Update `README.md` and `AGENTS.md` for the final six intent areas under `content/`.
- Publish or refresh `content/documentation/contributing.md` and adopting guidance.

### Phase 3 — Changelog and Boundaries

- Complete CHANGELOG entries for remaining visible moves.
- Confirm `docs/` and `specs/` are excluded from public navigation.

### Phase 4 — Final Audit

- Inventory routes, orphans, metadata and canonical sources.
- Run `npm run docs:validate` and `npm run docs:build`.
- Interactive review of representative landings, sidebars and bridges.

### Phase 5 — Residual Debt

- Record Mermaid `manualChunks` under `docs/technical-debt.md`.
- List any out-of-scope findings without expanding architecture.

## Validation Commands

```bash
npm run docs:validate
npm run docs:build
git diff --check
```
